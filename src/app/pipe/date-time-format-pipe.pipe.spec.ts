import { DateTimeFormatPipePipe } from './date-time-format-pipe.pipe';

describe('DateTimeFormatPipePipe', () => {
  it('create an instance', () => {
    const pipe = new DateTimeFormatPipePipe();
    expect(pipe).toBeTruthy();
  });
});
